export function getDom() {
  return {
    statusEl: document.getElementById("status") as HTMLSpanElement | null,
    threadList: document.getElementById("thread-list") as HTMLUListElement | null,
    threadSearchInput: document.getElementById("thread-search") as HTMLInputElement | null,
    messageList: document.getElementById("message-list") as HTMLDivElement | null,
    importBtn: document.getElementById("import-btn") as HTMLButtonElement | null,
    importPanel: document.getElementById("import-panel") as HTMLDivElement | null,
    chooseFileBtn: document.getElementById("choose-file-btn") as HTMLButtonElement | null,
    importFileEl: document.getElementById("import-file") as HTMLSpanElement | null,
    passphraseInput: document.getElementById("passphrase-input") as HTMLInputElement | null,
    passphraseCount: document.getElementById("passphrase-count") as HTMLSpanElement | null,
    runImportBtn: document.getElementById("run-import-btn") as HTMLButtonElement | null,
    seedBtn: document.getElementById("seed-btn") as HTMLButtonElement | null,
    resetBtn: document.getElementById("reset-btn") as HTMLButtonElement | null,
    copyDiagBtn: document.getElementById("copy-diag-btn") as HTMLButtonElement | null,
    searchInput: document.getElementById("search-input") as HTMLInputElement | null,
    searchPrevBtn: document.getElementById("search-prev") as HTMLButtonElement | null,
    searchNextBtn: document.getElementById("search-next") as HTMLButtonElement | null,
    searchControlsWrapper: document.getElementById("search-controls-wrapper") as HTMLDivElement | null,
    searchCounter: document.getElementById("search-counter") as HTMLSpanElement | null,
    searchAllBtn: document.getElementById("search-all") as HTMLButtonElement | null,
    searchResults: document.getElementById("search-results") as HTMLDivElement | null,
    jumpDateInput: document.getElementById("jump-date") as HTMLInputElement | null,
    jumpBtn: document.getElementById("jump-btn") as HTMLButtonElement | null,
    jumpControls: document.getElementById("jump-controls") as HTMLDivElement | null,
    jumpToggle: document.getElementById("jump-toggle") as HTMLButtonElement | null,
    optionsBtn: document.getElementById("options-btn") as HTMLButtonElement | null,
    optionsMenu: document.getElementById("options-menu") as HTMLDivElement | null,
    mediaToggle: document.getElementById("media-toggle") as HTMLButtonElement | null,
    darkModeToggle: document.getElementById("dark-mode-toggle") as HTMLButtonElement | null,
    tabMessages: document.getElementById("tab-messages") as HTMLButtonElement | null,
    tabGallery: document.getElementById("tab-gallery") as HTMLButtonElement | null,
    messagesView: document.getElementById("messages-view") as HTMLDivElement | null,
    searchView: document.getElementById("search-view") as HTMLDivElement | null,
    galleryView: document.getElementById("gallery-view") as HTMLDivElement | null,
    galleryGrid: document.getElementById("gallery-grid") as HTMLDivElement | null,
    galleryFrom: document.getElementById("gallery-from") as HTMLInputElement | null,
    galleryTo: document.getElementById("gallery-to") as HTMLInputElement | null,
    galleryFromClear: document.getElementById("gallery-from-clear") as HTMLButtonElement | null,
    galleryToClear: document.getElementById("gallery-to-clear") as HTMLButtonElement | null,
    gallerySize: document.getElementById("gallery-size") as HTMLSelectElement | null,
    gallerySort: document.getElementById("gallery-sort") as HTMLSelectElement | null,
    tabScrapbook: document.getElementById("tab-scrapbook") as HTMLButtonElement | null,
    scrapbookView: document.getElementById("scrapbook-view") as HTMLDivElement | null,
    scrapbookTagSelect: document.getElementById("scrapbook-tag-select") as HTMLSelectElement | null,
    manageTagsBtn: document.getElementById("manage-tags-btn") as HTMLButtonElement | null,
    scrapbookMessageList: document.getElementById("scrapbook-message-list") as HTMLDivElement | null,
    tagManagerModal: document.getElementById("tag-manager-modal") as HTMLDivElement | null,
    closeTagManager: document.getElementById("close-tag-manager") as HTMLButtonElement | null,
    newTagName: document.getElementById("new-tag-name") as HTMLInputElement | null,
    tagColorPresets: document.getElementById("tag-color-presets") as HTMLDivElement | null,
    createTagBtn: document.getElementById("create-tag-btn") as HTMLButtonElement | null,
    tagList: document.getElementById("tag-list") as HTMLDivElement | null,
  };
}
